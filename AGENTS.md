# 执行约束

- 遵循“最小充分原则”：优先采用满足目标和验收标准的简洁方案；当任务风险、质量或复用需要时，可以增加必要步骤。
- 遵循“不变量驱动的规则设计（Invariant-driven Rule Design）”：优先记录在不同场景中始终成立的核心约束；只有核心约束无法覆盖时，才补充必要的案例或例外。

# 项目方向

## 目标

使用 Agent 工具完整复刻用户提供的自有餐饮小店 Logo，随后将完成的案例制作成闲鱼商品并进行上架和推广，在实际执行中持续学习。

## 流程

1. 复刻 Logo
2. 闲鱼相关

按照以上顺序执行。完成当前阶段并获得用户确认后，进入下一阶段。

## 当前阶段：复刻 Logo

- 参考 Logo 由用户提供，并归用户所有。
- 以完整复刻参考 Logo 为当前交付目标。
- 执行 Logo 复刻任务时，使用 `$recreate-logo` 作为唯一通用流程来源。
- 还原参考图的构图、图形、文字、字体效果、配色、比例、位置和间距。
- 优先完成当前 Logo 的复刻成品。
- 输出格式和尺寸按照用户当次要求确定。

## 闲鱼阶段

使用完成的 Logo 案例制作闲鱼商品所需内容，包括商品图片、标题、介绍、价格、上架和推广。

## 协作方式

- 每次只推进一个明确事项。
- 围绕当前事项直接产出可检查的结果。
- 根据用户反馈继续修改，直到用户确认完成。
- 项目文档只记录已经确认、当前需要执行的要求。

## 图片相关

- 图片分析和需求整理使用 `imagegen` 技能。
- 图片生成统一使用 `gpt-image-2`。
- 调用图片生成工具前，必须先与用户讨论并明确即将生成图片的整体方案和画面结构。
- 同时生成多张图片时，必须分别说明每张图片的方案。
- 每次调用图片生成工具前，必须先向用户汇总本次调用将生成的全部图片方案。
- 只有用户在该次方案汇总后明确回复“开始生图”，才能调用图片生成工具。
- 生图授权仅适用于该次已汇总的方案；后续新增、修改或再次生成图片时，必须重新讨论、汇总并获得授权。

## Skill 测试与维护

- 测试失败后不得直接修改 `$recreate-logo`。必须先向用户说明实际表现、预期表现、判断为 Skill 问题的依据、准备修改的具体规则、完整修复提示及可能影响的现有流程；只有用户确认修复方向后才能修改。修改后运行格式校验，重跑失败用例和已有回归用例。

## `$teach`

- 当前任务出现明确学习需求或实际阻塞时使用 `$teach`。
- 常规执行事项直接完成。
- 采用“实践 → 问题 → 学习 → 修改 → 验证”的方式。
- 确认掌握后再记录学习结果。
